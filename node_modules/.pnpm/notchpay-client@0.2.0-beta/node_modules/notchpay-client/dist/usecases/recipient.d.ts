import { AxiosInstance } from "axios";
import { NotchPayCollectionResponse, NotchPayCreateRecipientRequest, NotchPayCreateRecipientResponse } from "../types";
import { Usecase } from "./base";
export declare class RecipientUsecases extends Usecase {
    private readonly privateKey;
    constructor(client: AxiosInstance, privateKey: string);
    /**
     * @returns Promise with all recipients
     */
    getAll(): Promise<NotchPayCollectionResponse<any>>;
    /**
     * @param recipientData Informations about the recipient
     * @returns Promise with the created recipient's data
     */
    create(recipientData: NotchPayCreateRecipientRequest): Promise<NotchPayCreateRecipientResponse>;
    /**
     * Retrieve a particular recipient
     * @param id recipient ID
     * @returns
     */
    get(id: string): Promise<any>;
}
