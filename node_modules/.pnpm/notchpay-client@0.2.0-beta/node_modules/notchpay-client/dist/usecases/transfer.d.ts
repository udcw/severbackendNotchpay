import { NotchPayCollectionResponse, NotchPayInitializeTransferRequest, NotchPayInitializeTransferResponse, NotchPayTransfer } from "@/types";
import { Usecase } from "./base";
import { AxiosInstance } from "axios";
export declare class TransferUsecases extends Usecase {
    private readonly privateKey;
    constructor(client: AxiosInstance, privateKey: string);
    /**
     * Retrieve all transfers
     */
    getAll(): Promise<NotchPayCollectionResponse<NotchPayTransfer>>;
    /**
     * Initialize a tranfer request
     * @param transferData
     * @returns
     */
    initialize(transferData: NotchPayInitializeTransferRequest): Promise<NotchPayInitializeTransferResponse>;
    /**
     * Get a single transfer by reference
     * @param reference transaction reference
     * @returns
     */
    get(reference: string): Promise<any>;
}
